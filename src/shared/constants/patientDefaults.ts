import { PatientType } from '../types';

/**
 * Default patient type values used when creating new patient records.
 * Currently defaults to 'Policlínico' (policlinico).
 */
export const DEFAULT_PATIENT_TYPE_ID = 'policlinico';
export const DEFAULT_PATIENT_TYPE_LABEL = PatientType.POLICLINICO;
